[build]
  publish = "frontend"

# If your frontend needs to proxy API calls during development or on Netlify,
# you can set up redirects to your backend (once deployed on Render):
#
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-domain.com/:splat"
#   status = 200
#   force = true
